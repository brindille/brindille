import InteractiveComponent from 'lib/core/InteractiveComponent';

export default class ButtonTest extends InteractiveComponent {
  constructor($el) {
    super($el);
  }

  dispose() {
    super.dispose();
  }
}
